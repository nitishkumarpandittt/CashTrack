/**
 * Fixed grain overlay (z-999). The fractalNoise data-URI and the
 * 0.03 / multiply pairing are taken verbatim from the brief.
 */
function NoiseOverlay() {
  return <div className="noise" aria-hidden="true" />;
}

export default NoiseOverlay;
