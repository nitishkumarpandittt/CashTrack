/**
 * /dashboard/assistant          a new, unsaved conversation
 * /dashboard/assistant/<id>     an existing one
 *
 * The UI is rendered by ../layout.jsx so it survives the URL changing
 * between conversations; this page only exists to give the route its params.
 */
export default function AssistantPage() {
  return null;
}
